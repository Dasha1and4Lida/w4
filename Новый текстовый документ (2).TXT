#include <iostream>
#include <locale.h>

using namespace std;

int main() {
    setlocale(LC_ALL, "Russian");
    
    int number;
    int count = 0;
    
    cout << "[+] Введите число: ";
    cin >> number;
    
    
    if (number <= 0) {
        cout << "\n[!] Ошибка: число должно быть положительным!\n";
        return 1;
    }
    
   
    int originalNumber = number;
    
    
    while (number > 0) {
        count = count + 1;
        number = number / 10;
    }
    
    // Выводим результат
    cout << "\n[+] Количество цифр: " << count << endl;
    
    return 0;

}
